# poolux-api 生产环境变量（示例，勿提交真实 token）

# 站点公网地址（GHA 拉 bundle / 回传 APK 必须可达）
PUBLIC_BASE_URL=https://poolux.top

# GitHub：必须使用 owner/name，不要填写 https://github.com/... URL
# Fine-grained PAT：目标仓库权限至少 Contents: Read and write；Actions 需已启用
# 经典 PAT：使用 repo scope
GITHUB_REPO=your-name/Poolux
GITHUB_TOKEN=ghp_xxxxxxxx

# 可选
# PORT=3001
# 登录令牌签名密钥：至少 32 位随机字符串（openssl rand -hex 32）。
# 不配置时会随机生成，代价是每次重启所有登录失效。
# JWT_SECRET=change-me
# CORS_ORIGIN=https://poolux.top
#
# 首次启动会自动创建后台账号 admin：
#   * 配了下面这项 → 用你指定的口令
#   * 没配　　　　 → 随机生成一个强口令，只在启动日志里打印一次（请立刻抄下来）
# ADMIN_INITIAL_PASSWORD=change-me-too

# ── 数据目录（强烈建议配置，避免覆盖上传代码时把数据一起覆盖掉）──────────────
# 不配置时数据仍然放在 poolux-api/ 目录内（与旧版一致）。
# 配置后数据库、上传的图片/字体都在这个目录里，覆盖代码不会碰到它们。
# 建议放在网站目录之外，例如：
# POOLUX_DATA_DIR=/www/poolux-data
#
# 下面三项一般不用配置，只有在需要把某一部分单独放到别处时再启用：
# POOLUX_DB_FILE=/www/poolux-data/data.db
# POOLUX_UPLOADS_DIR=/www/poolux-data/uploads
# POOLUX_JOBS_DIR=/www/poolux-data/apk-jobs
